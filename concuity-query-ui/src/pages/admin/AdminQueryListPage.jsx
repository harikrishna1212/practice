// BRD Screen 4 — Admin: Query List (Approval Queue).
// Placeholder: the admin screens are the next build step (pending confirmation).
export default function AdminQueryListPage() {
  return (
    <div className="page">
      <div className="page-header">
        <h2>Approval Queue</h2>
      </div>
      <p>The Admin approval screens will be built in the next step.</p>
    </div>
  );
}
